<Window x:Class="ValidatePerson.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:ValidatePerson.Views"
        mc:Ignorable="d"
        Title="Validate Person" Height="450" Width="800">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <views:PersonView Grid.Row="1" Margin="5" DataContext="{Binding PersonViewModel}" />

        <Button Grid.Row="2" Width="100" Height="50" Content="Exit" 
                Command="{Binding ExitCommand}" />

    </Grid>
</Window>
